# Endpoint: /account/my-models/endpoint-agents\n\n## Method: POST\n\n**Summary:** Create Endpoint Agent\n\n**Description:** Register an agent running on an external OpenAI-compatible endpoint. Pollinations sends a short-lived agent run token instead of a stored bearer credential. Private is the default; public agents require an allowlisted account and become public after 12 hours. API keys require `account:keys`.\n\n